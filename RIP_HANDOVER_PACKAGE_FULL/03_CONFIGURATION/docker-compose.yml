services:

  recruitment-intelligence-platform:

    build: .

    container_name:
      recruitment-intelligence-platform


    ports:

      - "8000:8000"


    env_file:

      - .env.production


    volumes:

      - ./data:/app/data

      - ./backups:/app/backups


    restart: unless-stopped